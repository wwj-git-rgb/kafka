interface:
  display_name: "Kafka"
  short_description: "Repo-specific patterns and workflows for kafka"
  default_prompt: "Use the kafka repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true